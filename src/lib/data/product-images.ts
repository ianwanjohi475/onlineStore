/**
 * Real product photography, keyed by product slug.
 *
 * TO USE REAL IMAGES:
 *   1. Drop photos into `public/products/` (e.g. `public/products/freepods-4-pro.jpg`)
 *   2. Add an entry below: `"freepods-4-pro": "/products/freepods-4-pro.jpg",`
 *
 * Any product without an entry falls back to a generated studio render.
 * Square images (1:1) on a clean/transparent background look best.
 */
export const productImages: Record<string, string> = {
  // "freepods-4-pro": "/products/freepods-4-pro.jpg",
};
